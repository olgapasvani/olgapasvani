#include <iostream>
#include <string>
#include <fstream>
#include <vector>

#include "struct.h"
#include "person.h"
#include "professor.h"
#include "course.h"
#include "student.h"
#include "secretary.h"

int main(){
    Person *p;
    p->SetID(1);
    p->SetName("Nikos");
    p->SetSurname("Tade");
    p->SetAddress("Zografou");
    p->SetAge(20);
    Student *s = Student(1,)
    
    Secretary *s;
    s->setStudent

    Student *s = Student();

    return 0;
}